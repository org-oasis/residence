# Supabase — new key model (2025+)
# Dashboard → Project Settings → API Keys → "Publishable key"
# Never commit .env. Only the publishable key goes in the client bundle.
VITE_SUPABASE_URL=https://yawrnlaefvzsrpukufta.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Legacy key name (still read as fallback during rotation, remove when migrated).
# VITE_SUPABASE_ANON_KEY=eyJ...
