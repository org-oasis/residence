# Audit log — 2026-05-13 01:14 CET

Working tree only (no commits per user request).

## Baseline

- Branch: main, clean except 2 new blog dirs (intended)
- Build: ✓ 2.20 s after pass 2 edits
- Lint: ✓ 0 warnings
- 78 prerendered routes, 21 blog entries, sitemap 69 URLs

## Pass 1 — Duplicates & redundancy

- Duplicate object keys: 0 (the prior priceRange duplicate was already fixed)
- "Duplicate" imports detected: all confirmed as legit `import type` / `import` splits in 6 files. Merging via inline `type` modifier is cosmetic and risks `verbatimModuleSyntax`. **No action.**
- Repeated literals: only 1 hardcoded site URL (already centralized as `SITE_URL`)

## Pass 2 — Dead & unused code

- ESLint unused: 0
- Icons defined but never imported: **`MoreHorizontal` removed** (7 lines, `src/components/icons/index.tsx`)
- Unreferenced assets: `WIP/01.webp` (skipped — designer in-progress) + ~40 responsive variants `-480w.avif` / `-768w.avif` (generated by `scripts/generate-image-variants.mjs` but no `<img srcset>` consumer found in src/). **TODO**: either wire `<picture>` srcset to consume them, or drop the variants script.

## Pass 3 — CSS hygiene

- `!important`: 0
- Arbitrary Tailwind values: present but all justified (brand hex, viewport units, calc, child selectors)
- Custom CSS classes: `rdp-*` applied at runtime by `react-day-picker` (false-positive on grep), `oasis-dialog-overlay` applied by Radix
- **No action.**

## Pass 4 — Performance

- Hero LCP image: `fetchPriority="high"`, `loading="eager"`, `decoding="async"` ✓
- No web fonts (system stack) ✓
- date-fns: named imports, tree-shakable ✓
- Scroll listener: via `useSyncExternalStore` with `{ passive: true }` + cleanup ✓
- Animations: compositor-friendly (transform/opacity in keyframes) ✓
- **No action.**

## Pass 5 — SEO & structured data

- Canonical/hreflang/og:image/JSON-LD: ✓
- 0 noindex on prod pages
- Sitemap: 69 URLs, well-formed
- **TODO**: the 2 new blog titles are 78 chars before suffix → 100+ with suffix. Other blog titles are 75-91 chars with suffix. Google truncates at ~70. Either shorten frontmatter titles or remove the `seo.blogArticleSuffix` for blog articles to gain 22 chars.

## Pass 6 — Redirects & routing

- All `Navigate` redirects: legitimate single-hop fallbacks
- 0 hardcoded absolute URLs in src/
- 0 empty href/to
- Blog routes "unreachable" via grep are reached dynamically via `BLOG_ARTICLES.generated.ts` ✓
- **No action.**

## Pass 7 — Accessibility & semantics

- All `<img>` have `alt` ✓
- 0 clickable divs ✓
- 1 H1 per page ✓
- **TODO**: `/apartments` heading hierarchy jumps H1 → H3 (no H2). `ApartmentCard` reuses H3 on home page where the level is correct. Fix needs an `as` prop on the card to set the heading level per context.

## Pass 8 — Codebase conventions

- 0 console.log
- 1 TODO marker (legitimate)
- Only file > 800 lines is generated (excluded)
- Lowercase shadcn/ui filenames are intentional ✓
- **No action.**

## Summary

| Pass | Candidates | Applied | Deferred to TODO |
|---|---|---|---|
| 1 | 0 | 0 | 0 |
| 2 | 3 | 1 (MoreHorizontal) | 2 (variants, WIP) |
| 3 | 0 | 0 | 0 |
| 4 | 0 | 0 | 0 |
| 5 | 1 | 0 | 1 (title length) |
| 6 | 0 | 0 | 0 |
| 7 | 1 | 0 | 1 (H2 skip on /apartments) |
| 8 | 0 | 0 | 0 |
| **Total** | **5** | **1** | **4** |

## Working tree changes

```
M src/components/icons/index.tsx   (- 7 lines: MoreHorizontal removed)
```

## Gates after edits

- ✓ Lint clean
- ✓ Build green (2.20 s)

## TODO (deferred)

1. **Responsive variants unused** — `*-480w.avif` and `*-768w.avif` generated but no consumer. Decide: wire `<picture>` srcset on big images, or drop the script.
2. **Blog title length** — 78 chars frontmatter + 22 chars suffix = 100+. Consider shortening titles or dropping suffix on blog articles.
3. **`/apartments` heading hierarchy** — H1 → H3 skip. Add `as` prop on `ApartmentCard` so home renders H3, list page renders H2.
4. **`WIP/01.webp`** — verify with designer if still needed; safe to delete otherwise.
